# Git Change OldRemote To NewRemote commands

>$ git remote show origin

>$ git remote rm origin

>$ git remote add origin New_origin_url

>$ git add .

>$ git commit -m "First commit"

>$ git remote show origin

>$ git push origin master

